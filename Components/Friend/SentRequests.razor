@page "/friend-request/sent"
@inject IFriendRequestService FriendRequestService

<h3>درخواست‌های دوستی ارسالی</h3>

@if (requests == null)
{
    <p>در حال بارگذاری...</p>
}
else if (!requests.Any())
{
    <p>درخواستی ارسال نشده است.</p>
}
else
{

}

@code {
    private List<FriendRequest>? requests;
    private List<string> columns = new() { "ReceiverFamilyId", "SentAt", "Status" };
    protected override async Task OnInitializedAsync()
    {
        requests = new List<FriendRequest>();
    }


}
